namespace Hiveprint;

enum FileProtocol:byte { LOCALE = 0, NFS, SMB = 2 }

table Storage {
  id:int;
  name:string;
  description:string;
  hostname:string;
  ip:string;
  user:string;
  password:string;
  protocol:FileProtocol;
  protocol_version:short;
}

root_type Storage;
